import React, { useContext } from 'react'
import formatDistance from 'date-fns/formatDistance'
import DevContext from '../../context/Employees/DevContext';

const ApplyLeave = (props) => {
    return (
        <div className="maindev">
            <div className="maindev card  text-center">
            {/* Select the date for which employee want leaves after selecting send an notification to concern reporting manager */}

            {/* A json file should be their to maintain the reporting manager of which each employee, that will be easy to send the notification to cocern people */}
            
            </div>
        </div>
    )
}

export default ApplyLeave
